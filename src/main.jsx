import React from "react";
import ReactDOM from "react-dom/client";
import CollegeHub from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CollegeHub />
  </React.StrictMode>
);
